{
  "name": "ai-first-aid-assistant---gemini---final",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "start": "npm run dev",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "test": "vitest run && npm test --prefix frontend"
  },
  "dependencies": {
    "formidable": "^3.5.4",
    "groq-sdk": "^0.9.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "ISC"
}
